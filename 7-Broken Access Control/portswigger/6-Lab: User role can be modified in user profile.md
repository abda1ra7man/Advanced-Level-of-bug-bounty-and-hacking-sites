```markdown
# 🔹 **استغلال معلمات URL للوصول إلى حساب آخر (IDOR)** 🔹

## 📌 **الهدف:**  
تعديل **معامل `id` في URL** للوصول إلى بيانات مستخدم آخر، ثم استخراج **مفتاح API** الخاص بـ Carlos.

---

## **📍 الخطوات:**

### 1️⃣ **تسجيل الدخول**
- استخدم **بيانات تسجيل الدخول** المزوّدة.
- بعد تسجيل الدخول، انتقل إلى **صفحة حسابك الشخصي**:
  ```
  https://your-lab-url/account?id=your-username
  ```
- لاحظ أن **معامل `id` في الرابط يحتوي على اسم المستخدم**.

---

### 2️⃣ **إرسال الطلب إلى Burp Repeater**
- افتح **Burp Suite** وشغّل **Burp Proxy**.
- انتقل إلى حسابك واضغط على الطلب في **HTTP History**.
- اضغط **كليك يمين** على الطلب واختر **Send to Repeater**.

---

### 3️⃣ **تعديل معامل `id`**
- في **Burp Repeater**، غيّر قيمة `id` من اسم المستخدم الحالي إلى `carlos`:
  ```
  GET /account?id=carlos HTTP/1.1
  Host: your-lab-url
  ```
- اضغط على **Send** لمشاهدة استجابة السيرفر.

---

### 4️⃣ **استخراج مفتاح API**
- في استجابة الطلب، ابحث عن سطر يحتوي على **API Key**:
  ```
  "api_key": "abcd1234efgh5678"
  ```
- انسخ المفتاح **وقدمّه وفقًا لمتطلبات المعمل**.

---

## ✅ **الملخص:**
1. **سجّل الدخول** وانتقل إلى حسابك الشخصي.
2. لاحظ أن **`id` في URL يحتوي على اسم المستخدم**.
3. استخدم **Burp Suite** لإرسال الطلب إلى **Repeater**.
4. غيّر `id` إلى **carlos** في الطلب وأرسله.
5. **استخرج مفتاح API** من الاستجابة وقدمّه.

---

💡 **نصيحة أمنية:**  
**عدم الاعتماد على معلمات URL** للتحقق من هوية المستخدم! لازم يكون في **تحقق من الجهة الخلفية (Server-Side Authorization Checks)** لمنع ثغرات **IDOR (Insecure Direct Object References)**. 🚨
```
